// import { create } from 'zustand'

// interface IntroAnimationState {
//   loadingState: boolean;
//   dimensionState: 'loadiong' | 'complete'
// }

// const useIntroAnimationState = create<IntroAnimationState>((set) => {
//   loadingState: true;
//   updateState: () => set((state) => !state)

// })